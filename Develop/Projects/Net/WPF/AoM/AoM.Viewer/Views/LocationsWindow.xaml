<Window x:Class="AoM.Viewer.Views.LocationsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

        xmlns:controls="clr-namespace:AoM.Viewer.Controls"
        mc:Ignorable="d"
        Title="LocationsWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ListBox ItemsSource="{Binding Locations}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedLocation}"/>

            <StackPanel Orientation="Horizontal" Grid.Row="1">

                <Button Content="Add location" Command="{Binding AddCommand}"/>
                <Button Content="Save" Command="{Binding SaveCommand}"/>
            </StackPanel>


        </Grid>

        <controls:LocationControl Grid.Column="1" DataContext="{Binding SelectedLocation}"/>



    </Grid>
</Window>
